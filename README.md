Для использования на Kaggle:

1. Выбираем ускоритель T4 x2
2. !git clone https://github.com/defdet/animal_transport_agent.git
3. !cd animal_transport_agent && pip install -r requirements.txt
4. !cd animal_transport_agent && python -m serve_vllm_kaggle <- Для запуска vLLM сервера